<div><h1>Your matches 🔧⚒️⚙️</h1>
<article>
{{#each users}}
<div>
   <img src='{{this.imageUrl}}' class="match-img" alt='profile-picture' />
  <h5>Name: {{this.username}}</h5>

  <div class="match-chat"><a href='/chat-app/{{../id}}'>💬</a>
    <a href='/matchProfile/{{_id}}'>Profile</a>
  </div>
 
  <form action='/match/{{../id}}' method='post'>
    <input class="hide-input"type='text' name='matchedProject' value='{{../id}}' placeholder='{{../id}}' />
    <input class="hide-input"type='text' name='userId' value='{{_id}}' placeholder='{{_id}}' />
    <button type='submit' id="connect-btn">connect</button>
  </form>
</div>
{{/each}}
</article>
</div>
