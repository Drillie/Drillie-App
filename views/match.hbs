<div><h1>Your matches 🔧⚒️⚙️</h1>
<article>
{{#each users}}
<div>
   <img src='{{this.imageUrl}}' class="match-img" alt='profile-picture' />
  <h5>{{this.username}}</h5>

  <div class="match-chat"><a href='/chat-app/{{../id}}'>💬</a>
    <a href='/matchProfile/{{_id}}'>Profile</a>
  </div>
 
  <form action='/match/{{../id}}' method='post' onsubmit='disableButton()'>
    <input class="hide-input"type='text' name='matchedProject' value='{{../id}}' placeholder='{{../id}}' required />
    <input class="hide-input"type='text' name='userId' value='{{_id}}' placeholder='{{_id}}' required />
    <button type='submit' id="connect-btn">connect</button>
  </form>
</div>
{{/each}}
</article>
</div>
