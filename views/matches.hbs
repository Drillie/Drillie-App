<form action='/match' method='GET'>
  <label for='match'> Look for a match </label>
  <select name='match'>
    {{#each allProject}}
      <option value='{{projectname}}'>{{projectname}}</option>
    {{/each}}
  </select>

  <button type='submit'>Search</button>
</form>